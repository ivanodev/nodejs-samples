enum DataType {
  STRING = 'string',
  INTEGER = 'integer',
  NUMBER = 'number',
  FLOAT = 'float',
  DATETIME = 'datetime',
  DATE = 'date',
  TIME = 'time',
  HOUR = 'hour',
  BOOLEAN = 'boolean',
  TEXT = 'text',
  EMAIL = 'email',
  ENUM  = 'enum',
  MONEY = 'money',
  IMAGE = 'image',
  SET = 'set'
}

export {
  DataType
}